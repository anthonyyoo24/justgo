# Phase 02 — No-signup identity and recovery

## Snapshot

- **Status:** In progress — implementation and browser/backend verification complete; native UI verification partial; physical-device acceptance remains pending.
- **Updated / author:** September 17, 2026 / Codex.
- **Scope:** [Phase 02](../IMPLEMENTATION_PLAN.md#phase-02), PRD AC-01, Tech acceptance 1–4 and relevant security checks.
- **Dependency:** [Phase 01 handoff](phase-01-foundation.md), reread before implementation. Its original uncommitted baseline was subsequently committed as `fe0fa015f99f652388344ab9c2dee5bd6e8bfaf8` and pushed to the private GitHub repository `anthonyyoo24/justgo`.
- **Branch:** `phase-02-identity`. Implementation commit: [`fd48e48`](https://github.com/anthonyyoo24/justgo/commit/fd48e4844161b12bd26c4b19c117cc5de105e674).
- **Sequencing decision:** Anthony authorized phase 02 implementation with Apple enrollment/signing and physical-iPhone acceptance deferred. Later feature implementation may use the verified account boundary while phase 02 stays open. This does not waive device acceptance before valuable-data external testing or release.
- **Summary:** Custom identity API, real owner-scoped database tables, independent expiring sessions, idempotent bootstrap/renewal, revocable keys, approved transfers, durable rate limits, a Swift Keychain module, typed client storage/protocol and minimal recovery UI now exist. No signup, purchases, onboarding, journal data or silent account merging was added.

## What changed

| Area                                                                  | Implementation                                                                                                                                                                                                            |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `packages/contracts/src/identity.ts`                                  | Strict request, response and typed-error contracts; secrets never appear in successful session responses.                                                                                                                 |
| `apps/api/src/db/schema.ts`, `drizzle/0001_identity.sql`              | Users, composite-owner devices, hashed recovery credentials, independent sessions, transfer requests and durable rate buckets; indexes, constraints, grants and forced RLS.                                               |
| `drizzle/0002_identity_policy_read_paths.sql`                         | Forward migration consolidating read policies after advisor feedback; pre-auth digest lookups remain read-only.                                                                                                           |
| `apps/api/src/identity/`                                              | Authentication boundary, session issuance/rotation, bootstrap/recovery, key/device revocation, approved one-use transfer, retirement primitive and trusted deployment address handling.                                   |
| `apps/api/src/build-app.ts`, `config.ts`, `server.ts`, `db/client.ts` | Route registration, typed redacted failures, POST CORS, bounded environment settings, production rate-key requirement and identity-aware readiness.                                                                       |
| `apps/mobile/modules/justgo-keychain/`                                | Swift synchronizing recovery items; separate non-sync secure per-installation state; storage errors fail visibly. Auto-linked by Expo.                                                                                    |
| `apps/mobile/src/features/identity/`                                  | Typed vault/API/controller, persisted pending identity operations, explicit recovery selection, account switching, key display/revocation, transfer pairing/approval, device management and temporary browser test vault. |
| Mobile entrypoint and native harness                                  | Identity preview replaces the foundation screen; original foundation tests remain. Native launch harness now targets account readiness.                                                                                   |
| `.vercelignore`, package/lock files                                   | New contracts included in API upload; Expo Crypto pinned to 57.0.3. The modules API is imported from Expo without a direct `expo-modules-core` dependency.                                                                |
| `docs/IDENTITY.md`, plan/index/decision register and README           | Concrete defaults, setup, migrations, API contracts, recovery/retention rules, approved sequencing exception and remaining gates.                                                                                         |

## Decisions and invariants

Read [IDENTITY.md](../IDENTITY.md) for the complete protocol and setup.

- 256-bit random recovery/session credentials, SHA-256 digests in PostgreSQL, independent 7-day device sessions, renewal within 24 hours of expiry, 10-minute transfers.
- New device generates a 64-bit transfer code plus a separate 256-bit claimant secret. An authenticated existing device compares six verification digits and explicitly approves. Redemption binds the exact previously saved device/session/credential proposal. Replay cannot create a second session.
- Save the pending intent and recovery item before bootstrap; retries preserve IDs and tokens. Never treat a timeout, locked storage or rejected credential as permission to bootstrap a replacement account.
- Recovering with a key or another discovered credential is explicit. Late/multiple iCloud credentials are preserved and cannot overwrite or silently switch/merge the current account. A separately confirmed new empty account is available when recovery is not possible.
- Device revocation and recovery-credential revocation are independent. Account retirement revokes both and preserves tombstones so old credentials cannot resurrect the account. The confirmed deletion UI and vendor cleanup remain phase 09 work.
- `IdentityService.withSession` verifies the bearer credential and account/device status, then gives future features the same transaction with verified owner context. No caller-provided owner ID or privileged database runtime.
- Default Keychain access group is derived from the app’s signing identity. The permanent bundle ID, signing team and continuity of that access group remain physical-device prerequisites. No new Expo project or permanent identifier was invented.
- Browser credentials exist only in tab memory. The preview is a development test surface, not evidence of native persistence or a web release.

## Problems encountered and fixes

| Finding                                                                            | Fix / evidence                                                                                                                                                   |
| ---------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Drizzle’s first table migration recreated the already-provisioned `justgo` schema. | Used `CREATE SCHEMA IF NOT EXISTS` before the migration was successfully applied. Local and staging migrations passed.                                           |
| Raw Drizzle SQL returned timestamp strings.                                        | Explicit ISO conversion at response boundaries; shared schemas and integration tests passed.                                                                     |
| Jest could not resolve the contracts’ ESM `.js` source import.                     | Added the standard relative-source module mapping; mobile suites passed, and Metro web export also succeeded.                                                    |
| Expo’s `getRandomBytes` has a debugging fallback to `Math.random`.                 | Credential generation uses `getRandomValues`, which delegates to the secure implementation and fails if unavailable.                                             |
| Expo Doctor rejected a direct `expo-modules-core` dependency.                      | Imported the supported API from `expo` and removed the direct dependency; Doctor now passes 21/21. The transitive native version is unchanged.                   |
| Advisor reported overlapping permissive read policies.                             | Applied additive `0002` locally and on staging; no security findings or remaining policy warnings. Only informational unused-index notices remain on new tables. |
| Vercel’s upload allowlist excluded the new identity contract.                      | Added that exact source file; inspected 22 files / 631,834 bytes with secrets, native sources and local files excluded. Clean cloud install/build passed.        |
| A recovery key revoked by another device could remain cached locally.              | Refresh key metadata before displaying/creating a key and clear revoked saved keys. Dedicated client regression test passes.                                     |
| Illustration blending showed a white rectangle, then tinted container edges.       | Paint the hero background and apply multiply only to its artwork container; final narrow-screen screenshot showed seamless cream.                                |
| Direct artifact download returned 403.                                             | Used authenticated `eas build:download`; downloaded and retained the built app successfully.                                                                     |
| Global developer directory points to Command Line Tools.                           | Used command-local `DEVELOPER_DIR=/Applications/Xcode.app/Contents/Developer` for simulator operations, preserving the user’s global setting.                    |
| Computer Use could not inspect Simulator because the Mac was locked.               | Requested a manual unlock and left native UI verification pending. No alternate screen-capture method or local rebuild was used to bypass this.                  |

## Verification evidence

All evidence below was recorded on September 17, 2026. Test fixtures are disposable and contain no journal/contact/payment data.

| Check                                                  | Actual result                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| ------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Initial phase one baseline                             | `npm run check`, local migration/readiness and four existing database tests passed. GitHub’s initial foundation CI run also completed successfully.                                                                                                                                                                                                                                                                                                     |
| `npm run check` and focused checks after final changes | TypeScript, ESLint, formatting and unit/component suites passed. Final counts: API 8, mobile 24, contracts 2 = **34** unit/component tests.                                                                                                                                                                                                                                                                                                             |
| `npm run test:db`                                      | **14** PostgreSQL tests passed: foundation 4, identity 10. Real `justgo_runtime`, identity pool size 3, owner isolation, concurrent/lost-response bootstrap, independent sessions, rotation replay, expiry, revocation/tombstones, keys, approval/expiry/cancellation/replay, cross-account denial and durable rates.                                                                                                                                   |
| `npm run doctor -w @justgo/mobile`                     | **21/21** passed after dependency correction.                                                                                                                                                                                                                                                                                                                                                                                                           |
| `npm run export:web -w @justgo/mobile`                 | Passed; shared contracts, Expo Crypto, vault and recovery screen bundled.                                                                                                                                                                                                                                                                                                                                                                               |
| In-app browser, 390×844 and 320×568                    | Real API account creation; key recovery into the same account in a second tab; independent session renewal; explicit key revocation and rejection after reload; transfer pending before approval, matching verification digits, approved recovery into the same account; offline setup and keyboard Enter retry; recovery confirmation UI and no horizontal overflow (320px client and scroll widths). Final artwork/layout inspected after correction. |
| Deployed API smoke                                     | `/health`, `/ready`, bootstrap, exact bootstrap retry, second account, independent-device recovery, `/me`, cross-account device revocation denial, renewal and exact renewal retry, revoked-old-session denial and other-device continuity all passed. Both staging fixture accounts were retired afterward through the revocation primitive.                                                                                                           |
| Supabase advisors                                      | Security: no findings. Performance: overlapping-policy warnings fixed; five informational unused-index notices on fresh tables retained for known access paths. [Advisor explanation](https://supabase.com/docs/guides/database/database-linter?lint=0005_unused_index).                                                                                                                                                                                |
| Native module discovery/build                          | Expo autolinking resolved `JustGoKeychainModule`. EAS simulator build completed successfully; artifact downloaded and installed on the existing iPhone 17 / iOS 26.4 simulator.                                                                                                                                                                                                                                                                         |
| Native UI / Maestro                                    | **Partial native UI pass.** Follow-ups verified account bootstrap, relaunch, renewal and recovery-key behavior on the retained simulator. Transfer/device-management UI remains pending. The original attempt was blocked by the Mac lock; see follow-up evidence below. Maestro remains unrun.                                                                                                                                                         |
| Physical iPhone matrix                                 | **Not run**, by the approved deferral: reinstall, two iPhones/same iCloud account, different Apple account, locked/disabled/delayed sync, signing/access groups, real-device recovery-key/transfer and revocation.                                                                                                                                                                                                                                      |

Browser and service tests together exercise protocol behavior; they do not prove iCloud synchronization, Keychain access while locked, reinstall continuity or device signing.

### Simulator follow-up — September 17, 2026

The Mac was accessible on the follow-up attempt. The retained EAS binary initially exposed an iOS Metro resolution error: the contracts source re-exported `./identity.js`, which Metro could not resolve to `identity.ts`. Web used the compiled package export and therefore had not exposed this. The source now imports the real `.ts` path; TypeScript rewrites that path to `.js` for the Node build. Mobile type checking permits TypeScript import extensions. CI now includes `npm run export:ios -w @justgo/mobile` as a native bundle regression check.

After the fix, the native app created and connected account `8d1d2903` using its Keychain vault. Terminating and relaunching the app restored the same account, and tapping **Renew this session** succeeded. `npm run check` passed all 34 unit/component tests, and both iOS and web exports passed. Browser account creation and session renewal were also rechecked through the real local API. No Swift changes or new native compilation were needed.

Fix commit [`b303fb7`](https://github.com/anthonyyoo24/justgo/commit/b303fb7bb91bb56251a9026adfd5eae332f5adf2) passed [GitHub CI run 35277871318](https://github.com/anthonyyoo24/justgo/actions/runs/35277871318), including all 48 unit/component/database tests, web and iOS exports, type/lint/format checks and Expo Doctor.

With Anthony manually scrolling to the recovery panel, the following checks passed through the native UI:

- Show and hide a recovery key; recover the same account `8d1d2903` in a second, browser-based client using the key displayed by the native app.
- Revoke that key through the native confirmation panel. Both the simulator and the existing second-client session stay connected. A fresh browser session rejects the revoked key.
- Generate a replacement key after revocation. Sending JustGO to the iOS home screen and reopening it automatically hides the displayed key and preserves account continuity.

The swipe investigation reproduced the failure after explicitly raising Simulator and after resetting/reconnecting Computer Use. Its documented `drag(from, to)` call maps the coordinates correctly through the installed JavaScript wrapper and IPC client; the public API exposes no touch-swipe or gesture-duration override. Drag and wheel-scroll had previously failed on the iOS home screen too, while Anthony's manual swipe works in JustGO. The evidence points to the tool's native input delivery, but does **not** establish the exact native-handler cause. No installed tool code, system permissions or Simulator settings were changed. [Official Computer Use documentation](https://learn.chatgpt.com/docs/computer-use) lists iOS simulator testing as supported but supplies no fix for this specific gesture failure.

Transfer/device-management controls further down the native screen still need inspection. A further manual scroll was requested. This is a different blocker from the earlier Mac lock. The physical-device matrix remains deferred.

GitHub [phase two CI run 35274014833](https://github.com/anthonyyoo24/justgo/actions/runs/35274014833) passed against implementation commit `fd48e48`: clean dependency install, `npm run check`, database migration and integration tests, web export and Expo Doctor. It completed on September 17, 2026 at 21:00:57 UTC.

## Environment and deployment record

- **Staging database:** existing project `kmcpcauaxlgpevkkenwd`, Canada Central. Drizzle migrations `0001` and `0002` applied through the separate migration role with verified TLS. Runtime remains restricted.
- **Staging API:** [phase two preview](https://justgo-e1207ajek-anthony-youngshin-yoos-projects.vercel.app), deployment `dpl_FqGbYcCxJXGzpUHTnQhXYwnpekNm`. Vercel inspect confirmed **preview / Ready**, function region `yul1`. Deployment protection remains enabled. This is not a production release.
- **New server secret:** `IDENTITY_RATE_LIMIT_KEY`, saved as a Vercel preview Secret. Secret values and temporary smoke fixtures remain in ignored local files; do not commit them or bundle them into mobile.
- **Native build:** [EAS build 7fb7ae73](https://expo.dev/accounts/anthonyyoos-team/projects/justgo/builds/7fb7ae73-3144-458e-9f21-96263e94a6dd), profile `development-simulator`, Expo SDK 57, provisional `dev.justgo.foundation`, completed `2026-09-17T20:43:35.000Z`. Build metadata references the phase one Git commit because it compiled the uploaded working tree; it is not a claim that phase two was already in that commit.
- **Retained native app:** `.local/phase02-simulator/JustGO.app`. Existing simulator ID `A194AE2D-F06F-42EB-9804-0BE17A547594`, iPhone 17 / iOS 26.4. Only that simulator was booted; it was shut down after the blocked UI check. No heavy local native compilation was run. Existing phase one artifacts, Pods and DerivedData were preserved.
- **Historical HTML tracker:** `docs/IMPLEMENTATION_PLAN.html` is absent from this checkout despite older handoff references. Current plan/status were updated in Markdown. The historical HTML validator was not claimed as passing or recreated as unrelated feature work.

## Remaining work and next steps

1. **Finish the remaining simulator recovery UI smoke.** First launch, account readiness, relaunch, session renewal and the key checks above passed on the follow-ups. Resolve the Computer Use gesture limitation or use a manual swipe to reach transfer/device-management controls, then record their behavior. Start local DB/API and Metro with one worker and reuse the retained EAS binary. These JS/UI changes need no new native build.
2. **Finish physical acceptance when Apple setup is available.** Confirm the permanent identifier and signing/access group, register two iPhones, build the device profile, establish a reachable HTTPS staging route without bundling a protection secret, and execute every physical matrix item above. Preserve account separation when sync is delayed. Phase 02 stays open until these pass.
3. **Phase 03 may proceed under the sequencing exception.** Use the verified session boundary and public schemas. Build shared query/account-cache cancellation and navigation around authenticated account changes. Do not substitute a fake user, bypass recovery failures or grant paid access. Approved onboarding questions/branching are still a separate phase 03 product prerequisite.
4. **Before release:** complete identity security review, measured rate/capacity limits, retention/backup/deletion behavior and scheduled bounded maintenance. Do not delete credential tombstones or claim million-user capacity. Dependency audit findings from Expo/Drizzle tooling remain tracked for compatible upstream updates.

Rollback remains additive: revert the Vercel deployment independently and repair schema problems with new reviewed migrations. Do not reset a shared database, erase recovery history or deploy migration credentials.
