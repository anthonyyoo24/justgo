# Phase 01 — Foundation & implementation decisions

## Snapshot

- **Status:** Complete — all four phase 01 exit gates passed.
- **Updated / author:** September 17, 2026 / Codex, with Anthony's account/project decisions.
- **Scope:** [Phase 01](../IMPLEMENTATION_PLAN.md#phase-01); PRD §§2, 10, 13 and Tech §§2, 3, 12.
- **Dependencies:** None. Historical context: [planning baseline](00-planning-baseline.md) and [iOS release scope](01-ios-release-scope.md).
- **Branch / commits:** Git initialized on `master`; no starting or ending commit. All implementation/docs are uncommitted. Original workspace contained documentation only.
- **Environment:** macOS arm64, Node 24.18.0, npm 11.16.0, PostgreSQL 17; exact mobile/backend versions in [FOUNDATION.md](../FOUNDATION.md).
- **Summary:** Workspace, mobile foundation preview, API, real database isolation tests, a verified deployed staging API, Supabase staging and EAS linkage now exist. The EAS-built iOS app passed native launch and connectivity. No domain feature or user data was added.

## What changed

| Path / migration                                                                                | Change and behavior                                                                                                                         |
| ----------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------- |
| Root package/lock, TypeScript/ESLint/Prettier configs, `.github/workflows/ci.yml`               | npm workspaces, strict shared settings, single lock, exact direct versions, CI PostgreSQL 17 and starter verification                       |
| `apps/mobile/app.config.ts`, `eas.json`, `.env.example`                                         | Expo 57 development app, iOS 16.4 minimum, EAS profiles, linked existing Expo project, permanent identifier guard                           |
| `apps/mobile/src/app`, `components`, `features/foundation`                                      | Router launch screen; accessible connection probe, busy guard, timeout/cancellation, offline feedback and retry                             |
| `apps/mobile/src/theme/tokens.ts`, `assets/`                                                    | Typed Paper-derived tokens, exported medal art and SVG sources, licensed Inter weights                                                      |
| `apps/mobile/e2e/launch.yaml`                                                                   | Native development-client launch/readiness smoke harness; requires installed Maestro and a running native binary                            |
| `packages/contracts/src`                                                                        | Strict Zod liveness/readiness response contracts shared across app/API                                                                      |
| `apps/api/src/server.ts`, `build-app.ts`, `config.ts`, `diagnostics.ts`                         | Fastify entrypoint, injectable app factory, `/health`, `/ready`, CORS/security headers, validated env and redacted diagnostics              |
| `apps/api/src/db`, `scripts/provision.sql`, `scripts/migrate.ts`, `drizzle/0000_foundation.sql` | Separate roles/connections, verified TLS, reused bounded pool, transaction-local owner helper, reviewed Drizzle migration; no domain tables |
| `apps/api/tests`                                                                                | API/config/logging tests and actual-role RLS isolation/rollback/concurrency tests using disposable local fixtures                           |
| `scripts/local-database.mjs`                                                                    | Reproducible dedicated PostgreSQL cluster; missing-only environment generation, random ignored credentials                                  |
| `.gitignore`, `.vercelignore`, `.easignore`                                                     | Ignore credentials/native/build outputs; explicit deployment upload allowlist                                                               |
| `docs/DESIGN.md`, `design-source/`, `DECISIONS.md`, `APPLE_SETUP.md`, `FOUNDATION.md`, README   | Design provenance, platform/version matrix, account checklist, decision ownership and setup/operations                                      |

## Decisions and invariants

- Anthony approved a dedicated JustGo Supabase project in **anthonyyoo corp**, after the provider's $0/month creation quote. Ref `kmcpcauaxlgpevkkenwd`, Canada Central. Runtime and migration passwords remain outside Git.
- Dedicated Vercel project `justgo`; API workspace root and Node 24.x. Only runtime DB credentials belong in its preview environment.
- Anthony created the Expo project. Existing ID `53cf0560-8ab5-446a-9bc6-deb193b337b5` is linked to **@anthonyyoos-team/justgo**; no duplicate project was created. Authenticated `eas init --id … --non-interactive` and `eas project:info` both verified the link.
- Apple Developer enrollment is not started. `dev.justgo.foundation` is an unreserved simulator identifier, not an approved permanent identifier. Device/distribution profiles require `IOS_BUNDLE_IDENTIFIER`; the cloud simulator profile explicitly supplies the provisional simulator identifier.
- Owner-scoped queries must use `withOwner` with an identity derived from a verified server session. Phase 02 must implement that session boundary; this phase supplies no fake authentication.
- Paper's selected final row owns appearance. Raster-only exact values are explicitly unresolved. Foundation copy is development copy, not approved onboarding. No Levels/filter/dictation/Day note features were introduced; full Progress scope remains in the plan.

## Problems encountered and fixes

| Symptom                                                                                     | Cause / fix                                                                                                                                                                     | Verification or limitation                                                                                                                                                                                                                                                   |
| ------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Renderer dependency mismatch                                                                | A newer test-library dependency resolved a different React. Pinned compatible RNTL 13.3.3 and overrode React/DOM/renderer 19.2.3.                                               | Component tests and Expo Doctor pass.                                                                                                                                                                                                                                        |
| Migration schema permission denied                                                          | PostgreSQL checks database CREATE even for Drizzle's `CREATE SCHEMA IF NOT EXISTS`. Granted CREATE only to migrator.                                                            | Local and staging migration succeeded; runtime still fails DDL/set-role tests.                                                                                                                                                                                               |
| Pooler TLS failed                                                                           | Trusted CA was missing and inferred hostname was not the dashboard's endpoint. Read the actual pooler address and supplied Supabase's CA with certificate verification enabled. | Restricted runtime reached staging successfully over TLS.                                                                                                                                                                                                                    |
| Vercel upload included generated native and ignored local files in dry run                  | CLI upload selection did not honor the intended Git exclusions. Added explicit `.vercelignore` allowlist before any upload.                                                     | Actual upload contained 18 API/workspace files, about 586 KB (including contracts build configuration); no secrets/native/design files.                                                                                                                                      |
| Vercel could not detect Fastify entrypoint                                                  | Detection requires the entrypoint's direct Fastify import. Split app factory into `build-app.ts` and passed Fastify from `server.ts`.                                           | Preview deployment built successfully from clean `npm ci`.                                                                                                                                                                                                                   |
| First Vercel target was production                                                          | Vercel classified a first unspecified deployment as production; that build failed before publishing.                                                                            | Subsequent command uses explicit `--target=preview`; no successful production deployment.                                                                                                                                                                                    |
| Expo initializer could not edit dynamic config                                              | `app.config.ts` requires an explicit owner value. Added only the confirmed project ID and returned owner, preserving settings.                                                  | `eas init` succeeded and `project:info` matched the supplied ID.                                                                                                                                                                                                             |
| Illustration had white rectangle in web                                                     | Multiply needs a painted parent background. Added cream background to hero container.                                                                                           | In-app browser screenshot verified seamless artwork.                                                                                                                                                                                                                         |
| One API test run exceeded the default timeout during concurrent native/download/export work | Transient heavily loaded host; no code change or timeout relaxation.                                                                                                            | Focused rerun passed all five tests in under a second.                                                                                                                                                                                                                       |
| Native build had no eligible destination                                                    | Installed iOS 26.4 simulator did not match Xcode 26.6's required 26.5 platform.                                                                                                 | Installed matching platform. Its first shared-cache generation and an 8 GB host caused heavy resource contention. Stopped local compilation at the owner’s request and retained caches. EAS compilation succeeded; the downloaded binary launched on one iOS 26.4 simulator. |

Additional deployment fixes: contracts now emit JavaScript during root `npm ci` preparation so Node/Vercel can resolve package exports. Fastify listener startup must not be awaited at module scope under Vercel's interception; removing that wait fixed the cold-start stall. The configured region was corrected to supported `yul1` (Montréal), verified in the deployment.

## Verification evidence

Recorded commands ran on September 17, 2026:

| Command / scenario                                                                         | Actual result                                                                                                                                                                                                                                    |
| ------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `npm run check`                                                                            | Strict typecheck, ESLint, Prettier and 16 unit/component tests passed (API 5, mobile 9, contracts 2), rerun after the simulator profile/configuration changes with local services stopped.                                                       |
| `npm run test:db`                                                                          | Four PostgreSQL 17 tests passed: effective role restrictions, two-owner RLS/write checks, rollback/context reset, concurrent owner contexts through pool size 1.                                                                                 |
| `npm run export:web -w @justgo/mobile`                                                     | Succeeded, only three selected Inter font weights included.                                                                                                                                                                                      |
| `npm run doctor -w @justgo/mobile`                                                         | 21/21 checks passed.                                                                                                                                                                                                                             |
| Drizzle migration using the separate staging session connection                            | Applied `0000_foundation`; restricted runtime readiness query succeeded through transaction pooler with verified TLS.                                                                                                                            |
| Supabase security and performance advisors                                                 | Both returned no findings after the foundation migration.                                                                                                                                                                                        |
| `eas init --id 53cf0560-8ab5-446a-9bc6-deb193b337b5 --non-interactive`; `eas project:info` | Existing link verified as @anthonyyoos-team/justgo.                                                                                                                                                                                              |
| Vercel clean cloud installation/build                                                      | `npm ci` succeeded from the root lock; preview deployment reached Ready.                                                                                                                                                                         |
| In-app browser at localhost:8081, 390×844 and 320×568                                      | Loaded fonts/artwork and readable layout; narrow screen scroll reached button without horizontal overflow; busy button disabled; ready message; stopping API showed generic offline failure; restarting and keyboard Enter retried successfully. |

Final plan verification: `node docs/checks/check-plan.mjs` passed all **12 checks**, including saved completion/gates/handoff consistency. A fresh in-app-browser view showed Phase 01 Complete, all four gates checked, **1 / 9** launch stages complete, and Phase 02 as the next stage. The temporary browser tab and docs server were then closed.

Native startup and connectivity passed manually through Computer Use at 11:56 EDT on September 17, 2026. Exactly one iPhone 17 / iOS 26.4 simulator was running. The app displayed the foundation artwork, Inter/body and Baskerville/headline typography, safe-area layout and connection control without a startup error. Tapping **Check connection** displayed **Connected. Everything is ready.** The local API logged both probes as HTTP 200. Deployed endpoint responses are independently verified. Maestro automated smoke has not run; installing a harness is not passing evidence. Physical-device identity/reinstall/two-phone sync, purchases and Android are outside phase 01 and have not been tested. No CI workflow execution is claimed: there is no connected Git remote yet; local checks and Vercel's clean cloud install/build are recorded separately.

Deployed staging API: `https://justgo-303esikf7-anthony-youngshin-yoos-projects.vercel.app` (deployment `dpl_Bp5dBNNJ7QPWtptrK96KAVZvEx5m`). Authenticated `vercel curl /health` and `/ready` returned 200 JSON matching the shared schemas: `{"status":"ok","service":"justgo-api"}` and `{"status":"ready"}`. Function region is Montréal (`yul1`). Vercel deployment protection remains enabled.

## Setup, data and operations

Read [README](../../README.md) and [FOUNDATION.md](../FOUNDATION.md) for exact commands, environment names, connection modes, migration order, deployment entrypoints and safe diagnostic boundaries. Administrative provisioning runs once, then Drizzle controls application migration history. There is no domain seed or shared fixture. Test tables are uniquely named and removed; tests refuse remote DB URLs.

Never deploy `MIGRATION_DATABASE_URL`, local credential files or mobile environment values containing secrets. Retain certificate verification. Database/runtime credential rotation should update the provider secret store and roll a new preview deployment. Use reviewed forward migrations and compatible API changes; do not reset a shared database for local tests. Roll back the Vercel deployment independently of a database forward repair. Monitoring alerts, capacity load tests, recovery/restore drills and production budget policy remain later work; no throughput/SLA claim is made.

### Memory-constrained native workflow

At Anthony’s request, gracefully interrupted the local Xcode build, shut down all simulators, quit Simulator and stopped Metro/API/docs servers. Preserved generated iOS/Pods, DerivedData, logs and completed work. Switched to the existing EAS `development-simulator` profile; no heavy local rebuild is permitted as an unexplained fallback. Added simulator-only public environment values and `.easignore`. Inspected the archive: 63 files / 1,253,560 bytes, no local secrets, native output, dependencies or docs. Four focused app-config tests pass, including the actual simulator profile. EAS completed build `2c73c1af-3d5d-4216-8f25-875c72691973` at `2026-09-17T15:43:21.932Z` using the existing profile, without an account/quota blocker. [Build page](https://expo.dev/accounts/anthonyyoos-team/projects/justgo/builds/2c73c1af-3d5d-4216-8f25-875c72691973). Downloaded the 59,333,741-byte artifact to `.local/JustGO-eas-simulator.tar.gz` and extracted `.local/eas-simulator/JustGO.app`. Installed it on the existing iPhone 17 simulator (`A194AE2D-F06F-42EB-9804-0BE17A547594`), iOS 26.4, bundle `dev.justgo.foundation`, version 0.1.0 / Expo SDK 57. Metro ran with one worker; no heavy checks ran concurrently.

The initial Metro connection failed because Node bound localhost to `::1` while the simulator requested `127.0.0.1`. Restarted only Metro with `NODE_OPTIONS=--dns-result-order=ipv4first`; the same binary loaded successfully, without any native rebuild. Reuse this installed/downloaded development build for JavaScript, styling and UI changes. Native dependency/configuration changes require a new EAS build.

After the successful native check, quit Simulator and confirmed zero booted devices; stopped Metro, API and the dedicated local PostgreSQL server. Preserved the installed app, downloaded artifact, native projects, Pods, DerivedData and build logs.

## Remaining work and risks

| Item                                                                           | Owner / required phase                                                 |
| ------------------------------------------------------------------------------ | ---------------------------------------------------------------------- |
| Permanent bundle ID, Apple enrollment and physical signing                     | Anthony / before 02 device recovery validation                         |
| Onboarding/catalog/reflection/paywall decisions                                | Anthony / see [decision register](../DECISIONS.md)                     |
| Expo/Drizzle tooling: 18 moderate audit findings, no high/critical at install  | Implementation owner / review compatible upstream fixes before release |
| Raster-only design measurements and neutral feeling artwork                    | Anthony + implementer / relevant feature phases                        |
| Provision production, finalize alerts/budgets/backup restore and load evidence | Anthony + implementer / 10                                             |

## Next phase: read this first

1. Phase 01 is complete. Phase 02 begins with reviewed users/devices/recovery/session migrations, RLS policies and server-verified ownership, building on `apps/api/src/db/client.ts`.
2. Re-run `npm run check`, `npm run test:db`, and API readiness. Confirm the permanent bundle ID and Keychain entitlement strategy before physical-device tests.
3. Preserve no-signup identity, independent sessions, conditional iCloud recovery, transaction-local context, redacted diagnostics and no persistent client journal. Read PRD and Tech before implementing; the preview grants no entitlement bypass.

## Record updates

Plan, handoff index and HTML source carry the same Complete phase 01 status. Historical planning records remain historical. Browser-local checklist values are a convenience and may differ from the saved handoff; no browser fixture is implementation evidence. Tech Stack links to the concrete version/setup record; product behavior is unchanged.
